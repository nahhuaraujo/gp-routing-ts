import styled from 'styled-components';

export const Logout = styled.button`
  padding: 0.5rem 1rem;
  border: none;
  background-color: transparent;
  color: inherit;
  font-size: inherit;
  cursor: pointer;
`;
