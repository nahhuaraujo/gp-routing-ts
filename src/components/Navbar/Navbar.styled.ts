import styled from 'styled-components';
import { darkGreen } from '../../styles/global-styled-css';

export const Navbar = styled.header`
  background-color: ${darkGreen};
  display: flex;
  justify-content: space-between;
  height: 100px;
`;
